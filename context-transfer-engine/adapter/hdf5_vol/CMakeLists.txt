# ------------------------------------------------------------------------------
# Clio HDF5 VOL Connector
# Requires: CLIO_CTE_ENABLE_HDF5_VOL=ON and HDF5 >= 1.14
#
# NOTE: the VOL connector API this source targets (H5VL_VERSION 3, the
# multi-dataset read/write callback signatures in H5VLconnector.h) is already
# present and ABI-stable in HDF5 1.14.x. The previous ">= 2.0.0" floor was
# over-conservative and excluded every HDF5 that ships with the MiV stack
# (1.14.6 conda). Lowered to 1.14.0 so the connector builds against the same
# HDF5 that neuroh5/MiV-Simulator link against.
# ------------------------------------------------------------------------------

if(NOT HDF5_FOUND)
  message(WARNING "HDF5 not found. HDF5 VOL adapter will not be built.")
  return()
endif()

if(HDF5_VERSION VERSION_LESS "1.14.0")
  message(WARNING "HDF5 ${HDF5_VERSION} found but >= 1.14.0 required for VOL connector. "
                  "HDF5 VOL adapter will not be built.")
  return()
endif()

message(STATUS "Building HDF5 VOL adapter (clio_hdf5_vol)")

add_library(clio_hdf5_vol SHARED
    clio_vol.cc
)

target_include_directories(clio_hdf5_vol PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    # The CTE root, so shared adapter headers resolve as "adapter/<name>.h" --
    # the same spelling the VFD already uses for adapter/cfs/cfs_io.h. The
    # config-string grammar is shared between the two connectors and must not be
    # duplicated: one CLIO config dialect, one parser.
    ${CMAKE_CURRENT_SOURCE_DIR}/../..
    # clio_cte_compressor_client is linked below, but its own include directory
    # is PRIVATE, so consumers do not inherit it. clio_vol.cc includes
    # clio_cte/compressor/compressor_client.h directly and needs the path
    # spelled out here.
    ${CMAKE_CURRENT_SOURCE_DIR}/../../compressor/include
    ${HDF5_INCLUDE_DIRS}
    ${HDF5_C_INCLUDE_DIRS}
)

# GPU-pointer passthrough (issue: apps writing/reading a device buffer through
# H5Dwrite/H5Dread). ctp::cuda_cxx is an INTERFACE target that carries
# CTP_ENABLE_CUDA=1 plus the real CUDA runtime; without it, gpu_api.h's
# IsDevicePointer()/DeviceAwareMemcpy() compile down to their CPU-only
# fallback (always false / always std::memcpy), same as mem_bdev_transport.cc.
set(VOL_GPU_LIBS "")
if(CLIO_CORE_ENABLE_CUDA)
    list(APPEND VOL_GPU_LIBS ctp::cuda_cxx)
elseif(CLIO_CORE_ENABLE_ROCM)
    list(APPEND VOL_GPU_LIBS ctp::rocm_cxx)
endif()

target_link_libraries(clio_hdf5_vol
    clio_cte_core_client
    clio_cte_core_runtime
    clio_cte_compressor_client
    clio::run::admin_runtime
    clio::run::admin_client
    clio::run::bdev_runtime
    ${VOL_GPU_LIBS}
    clio::run::bdev_client
    ctp::cxx
    ${HDF5_LIBRARIES}
    ${CMAKE_THREAD_LIBS_INIT}
)

# Same compile-time path trace as the compressor (see its CMakeLists). The
# connector half answers "was this write intercepted at all", which the
# compressor half cannot: a write that never reaches the compressor leaves no
# trace there by definition.
if(CLIO_NEUROPRESS_PATH_TRACE)
  target_compile_definitions(clio_hdf5_vol PRIVATE CLIO_NEUROPRESS_PATH_TRACE)
endif()

set_target_properties(clio_hdf5_vol PROPERTIES
    CXX_STANDARD 17
    CXX_STANDARD_REQUIRED ON
    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin
    LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin
    # Export all symbols so the DLL exposes H5VL_clio_register (and friends)
    # to tests/consumers on Windows, where symbols are hidden by default.
    WINDOWS_EXPORT_ALL_SYMBOLS ON
)

install(TARGETS clio_hdf5_vol
    LIBRARY DESTINATION lib
    ARCHIVE DESTINATION lib
)

message(STATUS "HDF5 VOL adapter configured successfully")

#------------------------------------------------------------------------------
# Examples
#------------------------------------------------------------------------------
# The NeuroPress examples are kept out of the repository (see .gitignore), so
# this is guarded: present in a working tree that has them, a no-op elsewhere.
if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/example/CMakeLists.txt)
  add_subdirectory(example)
endif()
